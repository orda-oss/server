meta {
  name: 7. Leave Voice Channel
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/voice/channels/{{voiceChannelId}}/leave
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "user_id": "{{userId}}"
  }
}

settings {
  encodeUrl: true
}
