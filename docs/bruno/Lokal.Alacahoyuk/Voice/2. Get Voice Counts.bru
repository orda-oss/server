meta {
  name: 2. Get Voice Counts
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/voice/counts
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
}
